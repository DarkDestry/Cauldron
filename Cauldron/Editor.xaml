<Window x:Class="Cauldron.Editor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:customControls="clr-namespace:Cauldron.CustomControls"
        mc:Ignorable="d"
        Icon="Cauldron.png"
        Style="{StaticResource DarkTheme}"
        Title="Cauldron Editor" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="ListBox" x:Key="HierarchyListBox">
            <Setter Property="Background" Value="#FF252526" />
            <Setter Property="TextElement.Foreground" Value="#f1f1f1" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <customControls:Control_HierarchyWindow/>
        <customControls:Control_PropertiesWindow Grid.Column="1" />

    </Grid>
</Window>
